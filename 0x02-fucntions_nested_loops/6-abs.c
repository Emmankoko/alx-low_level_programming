#include "main.h"
/**
 *  _abs - computes the absolute value of an integer
 *
 * Return:  integer value as the absolute of a number
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else 
		return (n * -1);
}	
