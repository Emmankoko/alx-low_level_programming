#include "main.h"

/**
 * mul - multiplies two integers
 * @a: one of the arguments passed to the function
 * @b: one of the arguments passed to the function
 * Return: an integer value as the product of the two argumenst
 */

int mul(int a, int b)
{
	return (a * b);
}
